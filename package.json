{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^4.2.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "gsap": "^3.11.5",
    "three": "^0.151.3"
  }
}
